import { PagePlaceholder } from "@/app/components/PagePlaceholder";

export const metadata = { title: "Tasks · ManagerOX CRM" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Tasks"
      icon="task_alt"
      description="Follow-ups, calls and reminders across every lead, contact and deal you own."
    />
  );
}
