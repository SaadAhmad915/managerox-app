import { PagePlaceholder } from "@/app/components/PagePlaceholder";

export const metadata = { title: "More · ManagerOX CRM" };

export default function Page() {
  return (
    <PagePlaceholder
      title="More"
      icon="more_horiz"
      description="The remaining sections, reachable from the phone tab bar."
    />
  );
}
