import { PagePlaceholder } from "@/app/components/PagePlaceholder";

export const metadata = { title: "Calendar · ManagerOX CRM" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Calendar"
      icon="calendar_month"
      description="Meetings and scheduled follow-ups, with the week and month views your team expects."
    />
  );
}
