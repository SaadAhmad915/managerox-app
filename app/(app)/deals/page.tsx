import { PagePlaceholder } from "@/app/components/PagePlaceholder";

export const metadata = { title: "Deals · ManagerOX CRM" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Deals"
      icon="work"
      description="Deal records with value, stage, owner and close date, shown as a board or a table."
    />
  );
}
