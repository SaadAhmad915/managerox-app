import { PagePlaceholder } from "@/app/components/PagePlaceholder";

export const metadata = { title: "Leads · ManagerOX CRM" };

export default function Page() {
  return (
    <PagePlaceholder
      title="Leads"
      icon="group"
      description="Lead capture, assignment and stage tracking will live here, backed by the Laravel API."
    />
  );
}
